lombok.var.flagUsage = ALLOW     #用于启用var变量，val默认是启用的
lombok.accessors.chain = false   #在set方法中返回this，默认false
lombok.accessors.fluent = false   #get,set方法名称使用字段名称本身，而不是使用标准的set，get前缀
#lombok.accessors.prefix += M #在get，set方法的set，get之后加上前缀